interface:
  display_name: "Goosnav Software Productization"
  short_description: "Ship a reliable universal ZIP app first, then advance through explicit product gates."
  default_prompt: "Apply the Goosnav Software Productization Protocol. Default to M1a, preserve the working GUI, and produce the next verified slice of the universal ZIP with stable cross-platform launcher images."
policy:
  allow_implicit_invocation: true
