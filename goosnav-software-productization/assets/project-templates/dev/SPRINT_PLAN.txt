{{PROJECT_NAME}} — ACTIVE SPRINT PLAN
Last updated: {{DATE}}
ACTIVE MILESTONE: M1a
STATUS: ACTIVE

OBJECTIVE
---------
Ship one universal ZIP that a customer can extract and launch into the working browser GUI on every claimed OS/architecture without developer tooling.

IN SCOPE
--------
- Preserve and verify the primary GUI workflow.
- Place mutable product material beneath `app/`.
- Implement the manifest/bootstrap contract and external application-data paths.
- Generate the five stable launcher images.
- Bundle pinned `uv`; lock exact binary dependencies for the full matrix.
- Implement visible setup, readiness redirect, supervision, and stable exception codes.
- Assemble and test the explicit-whitelist universal ZIP.

NON-GOALS
---------
- DRM, activation, login, or copy prevention.
- Signing, installers, fully offline runtime, or automatic updates.
- CLI/TUI, SaaS, hub, storefront, or mobile implementation.

VERTICAL SLICES
---------------
[ ] M1a-S01 Repository reconnaissance, GUI launch path, and acceptance contract
[ ] M1a-S02 Customer root and mutable `app/` layout
[ ] M1a-S03 Manifest, bootstrap, loopback readiness, and external data paths
[ ] M1a-S04 Stable launcher images and bundled `uv` tools
[ ] M1a-S05 Versioned runtime, setup UI, retries, errors, and supervision
[ ] M1a-S06 Universal ZIP assembly and full target/failure matrix
[ ] M1a-S07 Candidate-for-acceptance evidence

VERIFICATION
------------
- Run every scenario in `references/zip-app-architecture.md` or its copied project acceptance contract.
- Confirm application-only changes leave all launcher-image hashes unchanged.
- Confirm private `dev/` and agent files are ignored and absent from the ZIP.

EXIT GATE
---------
Set M1a to CANDIDATE_FOR_ACCEPTANCE and stop. Only the user may set M1a to USER_ACCEPTED or start another gate.
